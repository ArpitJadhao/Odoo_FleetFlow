import './index.css'
import { AppRouter } from './router/AppRouter'

/**
 * App — root entry point.
 * AppRouter handles session restoration and all routing.
 */
function App() {
  return <AppRouter />
}

export default App
