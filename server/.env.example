# FleetFlow Server Environment Variables

PORT=4000
DATABASE_URL="postgresql://postgres:admin123@localhost:5432/fleetflow?schema=public"

# Supabase JWT secret — found in Supabase Dashboard → Settings → API → JWT Secret

